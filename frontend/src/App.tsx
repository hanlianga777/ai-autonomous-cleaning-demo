import { PrototypeWorkbench } from "@/components/prototype/PrototypeWorkbench";

/** The customer demo deliberately has one shell at `/` and `/prototype`. */
export default function App() { return <PrototypeWorkbench />; }
